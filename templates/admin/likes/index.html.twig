{% extends 'admin/base.html.twig' %}

{% block adm_body %}
    <main role="main" class="container">
        <h1 class="mt-5">
            Избранное
        </h1>
        <hr>
        {% for item in paginator.results %}
            {% include 'news/_item.html.twig' with {'item': item.news} %}
        {% else %}
            Пока вы не добавили нечего в избранное
        {% endfor %}
    </main>
{% endblock %}